# ========================================
# DATABASE CONFIGURATION
# ========================================
NODE_ENV=development
PORT=3003

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=12345678
DB_NAME=restaurant_saas

# ========================================
# JWT & AUTHENTICATION
# ========================================
# Generate secure random strings (at least 32 characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=91fe1b082aa516e0b50b805ab72d29e08d76da79e76f63452cbea5b0208360e6
REFRESH_TOKEN_SECRET=3d4311344a8728e9c605ebc956e3053bab203734047ad4cfb264e6b88b831d19

# JWT Expiration Times
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d

JWT_ISSUER=admin-saas
JWT_AUDIENCE=admin-saas-api


# ========================================
# SECURITY & RATE LIMITING
# ========================================
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ========================================
# CORS CONFIGURATION
# ========================================
# Frontend URLs that can access the API
CORS_ALLOWED_ORIGINS=http://localhost:5174,http://localhost:3003

# ========================================
# FILE UPLOAD CONFIGURATION
# ========================================


MAX_FILE_SIZE=10485760
UPLOAD_DIR=./public/uploads

# Cloud Storage (Optional - for production)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_S3_BUCKET=
# AWS_REGION=

# ========================================
# FRONTEND URLS
# ========================================
ADMIN_PANEL_URL=http://localhost:5174

# ========================================
# EMAIL CONFIGURATION (Optional)
# ========================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@yourplatform.com
SMTP_HOST=smtp.titan.email
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=info@mnapixel.com
SMTP_PASSWORD=NoahSenerSalahi1982!
SMTP_FROM=info@mnapixel.com
SMTP_FROM_NAME=Sifre Yenileme | MnaPixel
SMTP_ENCRYPTION=SSL


EMAIL_FROM_NAME=Admin SaaS
EMAIL_FROM_ADDRESS=info@mnapixel.com
REDIS_URL=redis://localhost:6379
# ========================================
# LOGGING
# ========================================
LOG_LEVEL=debug

IP_CHANGE_POLICY=block

# ======================
# API Key (Optional)
# ======================
# For server-to-server authentication (no-origin requests)
# Leave empty to disable
API_KEY=

AUTH_RATE_LIMIT_MAX=5
AUTH_RATE_LIMIT_WINDOW_MS=900000

# Global rate limit
GLOBAL_RATE_LIMIT_MAX=1000
GLOBAL_RATE_LIMIT_WINDOW_MS=900000
